# eval-mcp-server runtime dependency.
# The official Model Context Protocol Python SDK (stdio transport).
# Needed to RUN the MCP server (server.py) and the real-round-trip path of
# test_client.py. The eval LOGIC, the conformance suite, and the fallback path
# of test_client.py are stdlib-only and need none of this.
mcp>=1.2.0

# Optional: only for grade_rag(live=True) Anthropic faithfulness mode, which is
# lazy-imported and gated on ANTHROPIC_API_KEY. The default stdlib grader needs
# no key and no network. Uncomment to enable live mode.
# anthropic>=0.40.0
